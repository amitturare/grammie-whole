import { body, params, query } from "../utils/validator";

// import { ZHelper, ZHelperId, ZHelperWithoutId, ZHelperOptional } from "./helper.types";

// export const HelperValidations = [body(ZHelperWithoutId)];

// export const OptionalValidations = [query(ZHelperOptional)];

// export const IdValidations = [params(ZHelperId)];

// export const PutValidations = [body(ZHelperWithoutId), params(ZHelperId)];

// export const PatchValidations = [body(ZHelperOptional), params(ZHelperId)];
